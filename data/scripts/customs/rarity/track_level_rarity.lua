-- local trackLevel = Action()

-- function trackLevel.onUse(player, item, fromPosition, itemEx, target, toPosition, isHotkey)
--     local level = itemEx:getCustomAttribute("combatPowerLevel") or 0

--     print("Level:", level)
-- end

-- trackLevel:register(ITEM_ID)


-- local trackRarityLevel = Action()

-- function trackRarityLevel.onUse(player, item, fromPosition, itemEx, target, toPosition, isHotkey)
--     local rarityLevel = itemEx:getCustomAttribute("rarity") or 0

--     print("Level:", rarityLevel)
-- end

-- trackRarityLevel:register(ITEM_ID)